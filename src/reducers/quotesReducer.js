const initialState = {
  loading: false,
  quote: '',
  characterName: '',
  characterImage: '',
};

export default function reducer(state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }
}
